# Authored by Kimi Code (AI coding agent) — task CHRON-018.
# Extended by Kimi Code (AI coding agent) — task CHRON-025.
[package]
name = "palimpsest-sim-ai"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# Dependency allow-set (ADR-0017, enforced by tests/dependency_direction.rs):
# sim-world, sim-entity, sim-time, serde, and std only. Never sim-core,
# sim-events, sim-scheduler, sim-storage, godot-bridge, bevy_ecs, Godot, or
# any LLM crate.
[dependencies]
palimpsest-sim-time = { path = "../sim-time" }
palimpsest-sim-world = { path = "../sim-world" }
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
